= form_for @test123 do |f|
  - if @test123.errors.any?
    #error_explanation
      %h2= "#{pluralize(@test123.errors.count, "error")} prohibited this test123 from being saved:"
      %ul
        - @test123.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :age
    = f.number_field :age
  .actions
    = f.submit 'Save'
